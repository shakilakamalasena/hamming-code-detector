export const encodeHammingCode = (message: string): string => {
    return "Encoded " + message;
}

export const decodeHammingCode = (encodedMessage: string): string => {
    return "Decoded " + encodedMessage;
}
